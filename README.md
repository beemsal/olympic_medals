# Olympic Medals Screen App
Requires node > v.6.9

install

```npm install```

start dev mode

```npm start```

served at

```http://localhost:8080```

run test:

```npm test```

run test in interactive mode

```npm run test:tdd```
